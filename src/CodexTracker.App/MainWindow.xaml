<Window x:Class="CodexTracker.App.MainWindow"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
 Title="Codex Tracker" Icon="Assets/App.ico" Width="1060" Height="850" MinWidth="820" MinHeight="620" WindowStartupLocation="CenterScreen" WindowStyle="None" ResizeMode="CanResize" UseLayoutRounding="True" Background="{StaticResource BackgroundBrush}" Foreground="{StaticResource TextBrush}" FontFamily="Segoe UI Variable, Segoe UI">
 <shell:WindowChrome.WindowChrome><shell:WindowChrome CaptionHeight="47" ResizeBorderThickness="6" CornerRadius="12" GlassFrameThickness="0"/></shell:WindowChrome.WindowChrome>
 <Border BorderBrush="#2A384C" BorderThickness="1">
 <Grid>
  <Grid.RowDefinitions><RowDefinition Height="48"/><RowDefinition Height="*"/><RowDefinition Height="42"/></Grid.RowDefinitions>
  <Border Background="#101925" BorderBrush="#243043" BorderThickness="0,0,0,1">
   <Grid Margin="22,0,6,0"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
     <Border Width="24" Height="24" Background="#1A463F" CornerRadius="7" Margin="0,0,10,0"><TextBlock Text="&gt;_" Foreground="{StaticResource AccentBrush}" FontFamily="Cascadia Code, Consolas" FontSize="12" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
     <TextBlock Text="Codex" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/><TextBlock Text=" Tracker" Foreground="{StaticResource MutedBrush}" FontSize="14" VerticalAlignment="Center"/>
     <Border Margin="12,0,0,0" BorderBrush="#324354" BorderThickness="1" CornerRadius="4" Padding="5,2" Visibility="{Binding IsDemo, Converter={StaticResource BoolVisibility}}"><TextBlock Text="DÉMO" FontSize="8" Foreground="#89A6BC"/></Border>
    </StackPanel>
    <StackPanel Grid.Column="1" Orientation="Horizontal" shell:WindowChrome.IsHitTestVisibleInChrome="True">
     <Button Content="⚙" ToolTip="Réglages" AutomationProperties.Name="Réglages" Style="{StaticResource QuietButton}" Click="Settings_Click" FontSize="17" Padding="12,4" Margin="0,4,14,4"/>
     <Button Content="—" ToolTip="Réduire" AutomationProperties.Name="Réduire" Style="{StaticResource QuietButton}" Click="Minimize_Click" Padding="12,4" Margin="0,4"/>
     <Button Content="□" ToolTip="Agrandir" AutomationProperties.Name="Agrandir ou restaurer" Style="{StaticResource QuietButton}" Click="Maximize_Click" Padding="12,4" Margin="0,4"/>
     <Button Content="✕" ToolTip="Masquer dans la barre des tâches" AutomationProperties.Name="Masquer dans la barre des tâches" Style="{StaticResource QuietButton}" Click="Hide_Click" Padding="12,4" Margin="0,4"/>
    </StackPanel>
   </Grid>
  </Border>
  <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="30,27,27,20">
  <StackPanel>
   <Grid Margin="0,0,0,24"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
    <StackPanel><TextBlock Text="VOTRE ESPACE CODEX" Style="{StaticResource Eyebrow}" Foreground="{StaticResource AccentBrush}" Margin="0,0,0,8"/><TextBlock Text="Gardez le fil de vos quotas." FontSize="28" FontWeight="SemiBold"/><TextBlock Text="Tous vos comptes. Le bon moment pour reprendre." Foreground="{StaticResource MutedBrush}" FontSize="13" Margin="0,7,0,0"/></StackPanel>
    <Button Grid.Column="1" Content="↻   Actualiser" Click="Refresh_Click" IsEnabled="{Binding IsIdle}" VerticalAlignment="Center"/>
   </Grid>
   <Border Background="#182F32" BorderBrush="#2E6258" BorderThickness="1" CornerRadius="12" Padding="17" Margin="0,0,0,18" Visibility="{Binding ShowOnboarding, Converter={StaticResource BoolVisibility}}">
    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Un repère toujours à portée de vue" FontWeight="SemiBold" Foreground="#A4F1DF"/><TextBlock TextWrapping="Wrap" Foreground="#A6C5C0" Margin="0,6,20,0" Text="Près de l’horloge, ouvrez ^ puis faites glisser l’icône vers la zone visible. Le nombre affiché indique votre quota hebdomadaire restant."/></StackPanel><Button Grid.Column="1" Content="Compris" Click="Onboarding_Click" VerticalAlignment="Center"/></Grid>
   </Border>
   <Border Background="#3C3020" BorderBrush="#80653A" BorderThickness="1" CornerRadius="12" Padding="17" Margin="0,0,0,18" Visibility="{Binding HasPendingSwitch, Converter={StaticResource BoolVisibility}}">
    <StackPanel><TextBlock Text="Vérifiez le compte ouvert dans Codex" FontWeight="SemiBold" Foreground="#FFD391"/><TextBlock Text="{Binding PendingSwitchMessage}" TextWrapping="Wrap" Foreground="#D8C5A9" Margin="0,7,0,13"/><WrapPanel><Button Content="C’est le bon compte" Click="VerifySwitch_Click" IsEnabled="{Binding IsIdle}" Margin="0,0,10,0"/><Button Content="Restaurer la session précédente" Click="RestoreSwitch_Click" IsEnabled="{Binding IsIdle}"/></WrapPanel></StackPanel>
   </Border>
   <Grid Margin="0,0,0,25" Visibility="{Binding HasSelected, Converter={StaticResource BoolVisibility}}">
    <Grid.ColumnDefinitions><ColumnDefinition Width="1.85*"/><ColumnDefinition Width="16"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
    <Border BorderThickness="1" BorderBrush="#2C594F" CornerRadius="15" Padding="23">
     <Border.Background><LinearGradientBrush StartPoint="0,0" EndPoint="1,1"><GradientStop Color="#173A36" Offset="0"/><GradientStop Color="#14262D" Offset="0.6"/><GradientStop Color="#152330" Offset="1"/></LinearGradientBrush></Border.Background>
     <Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
      <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBlock Text="DANS VOTRE BARRE DES TÂCHES" Style="{StaticResource Eyebrow}" Foreground="#9ABDB6"/><Border Grid.Column="1" Background="#244E44" CornerRadius="5" Padding="6,3"><TextBlock Text="●  SUIVI" Foreground="#8BE8CC" FontSize="9" FontWeight="SemiBold"/></Border></Grid>
      <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,8,0,8"><TextBlock x:Name="HeroWeekly" Text="{Binding Selected.WeeklyNumber}" Foreground="{Binding Selected.WeeklyBrush}" FontFamily="Segoe UI" FontSize="48" TextOptions.TextFormattingMode="Ideal" TextOptions.TextRenderingMode="Grayscale" FontWeight="SemiBold"/><StackPanel VerticalAlignment="Center" Margin="12,8,0,0"><TextBlock Text="restant cette semaine" FontSize="13"/><TextBlock Text="{Binding Selected.Email}" Foreground="#A4BDBF" FontSize="12" Margin="0,6,0,0" TextTrimming="CharacterEllipsis" MaxWidth="270"/></StackPanel></StackPanel>
      <StackPanel Grid.Row="2"><ProgressBar Value="{Binding Selected.WeeklyPercent, Mode=OneWay}" Foreground="{Binding Selected.WeeklyBrush}" Height="5"/><TextBlock Text="{Binding Selected.Freshness}" Foreground="#8DAAAE" FontSize="10" Margin="0,10,0,0"/></StackPanel>
     </Grid>
    </Border>
    <Border Grid.Column="2" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="15" Padding="22">
     <StackPanel><TextBlock Text="PROCHAIN RESET HEBDOMADAIRE" Style="{StaticResource Eyebrow}"/><TextBlock Text="{Binding Selected.ResetCountdown}" FontSize="25" FontWeight="SemiBold" Margin="0,17,0,7"/><TextBlock Text="{Binding Selected.ResetExact}" Foreground="#C1CEDF" FontSize="12"/><TextBlock Text="{Binding Selected.ResetZone}" Foreground="{StaticResource MutedBrush}" FontSize="10" Margin="0,3,0,16"/><Border Background="#1B283B" CornerRadius="7" Padding="9,7"><TextBlock Text="{Binding Selected.ReserveSummary}" Foreground="#9DB4D4" FontSize="11"/></Border></StackPanel>
    </Border>
   </Grid>
   <Grid Margin="0,0,0,13"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><StackPanel Orientation="Horizontal" VerticalAlignment="Center"><TextBlock Text="Vos comptes" FontSize="18" FontWeight="SemiBold"/><Border Background="#202D41" CornerRadius="5" Margin="10,0,0,0" Padding="7,3"><TextBlock Text="{Binding AccountCount}" Foreground="#AEC0D9" FontSize="10"/></Border></StackPanel><StackPanel Grid.Column="1" Orientation="Horizontal"><Button Content="Importer le compte Codex" Style="{StaticResource QuietButton}" Click="Import_Click" IsEnabled="{Binding IsIdle}" Margin="0,0,8,0"/><Button Content="＋  Ajouter un compte" Style="{StaticResource PrimaryButton}" Click="Add_Click" IsEnabled="{Binding IsIdle}"/></StackPanel></Grid>
   <Border Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="14" Padding="38" Visibility="{Binding IsEmpty, Converter={StaticResource BoolVisibility}}"><StackPanel HorizontalAlignment="Center"><TextBlock Text="Un seul endroit pour tous vos comptes." FontSize="20" FontWeight="SemiBold" HorizontalAlignment="Center"/><TextBlock Text="Importez votre session Codex ou ajoutez un compte pour commencer." Foreground="{StaticResource MutedBrush}" Margin="0,12,0,8" TextAlignment="Center" TextWrapping="Wrap"/><TextBlock Text="Vos sessions restent chiffrées sur ce PC." Foreground="#68829B" FontSize="12" HorizontalAlignment="Center"/></StackPanel></Border>
   <ItemsControl ItemsSource="{Binding Accounts}"><ItemsControl.ItemTemplate><DataTemplate>
    <Border Background="{StaticResource PanelBrush}" BorderBrush="{Binding CardBorder}" BorderThickness="1" CornerRadius="12" Padding="18,16,18,14" Margin="0,0,0,11">
    <StackPanel>
     <Grid Margin="0,0,0,14"><Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
      <Border Background="{Binding AvatarBackground}" Width="35" Height="35" CornerRadius="10" Margin="0,0,12,0"><TextBlock Text="{Binding Initials}" Foreground="{Binding AvatarForeground}" FontSize="12" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
      <StackPanel Grid.Column="1" VerticalAlignment="Center"><WrapPanel><TextBlock Text="{Binding Email}" FontWeight="SemiBold" FontSize="13" Margin="0,0,10,0"/><Border Background="#20483E" CornerRadius="4" Padding="5,2" Margin="0,0,5,0" Visibility="{Binding IsActive, Converter={StaticResource BoolVisibility}}"><TextBlock Text="ACTIF DANS CODEX" Foreground="#80DABB" FontSize="8"/></Border><Border Background="#253C52" CornerRadius="4" Padding="5,2" Visibility="{Binding IsSelected, Converter={StaticResource BoolVisibility}}"><TextBlock Text="ICÔNE" Foreground="#A7D5F6" FontSize="8"/></Border></WrapPanel><TextBlock Text="{Binding Freshness}" Foreground="{Binding FreshnessBrush}" FontSize="10" Margin="0,4,0,0"/></StackPanel>
      <Border Grid.Column="2" Background="#202D40" CornerRadius="6" Padding="9,5" VerticalAlignment="Top"><TextBlock Text="{Binding Plan}" Foreground="#BCD0ED" FontSize="10" FontWeight="SemiBold"/></Border>
     </Grid>
     <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="1.12*"/><ColumnDefinition Width="1.12*"/><ColumnDefinition Width="1.55*"/><ColumnDefinition Width="1.1*"/></Grid.ColumnDefinitions>
      <StackPanel Margin="0,0,22,0"><TextBlock Text="SEMAINE" Style="{StaticResource Eyebrow}"/><StackPanel Orientation="Horizontal" Margin="0,5,0,8"><TextBlock Text="{Binding WeeklyNumber}" Foreground="{Binding WeeklyBrush}" FontSize="20" FontWeight="SemiBold"/><TextBlock Text=" restant" Foreground="{StaticResource MutedBrush}" FontSize="10" VerticalAlignment="Bottom" Margin="0,0,0,3"/></StackPanel><ProgressBar Value="{Binding WeeklyPercent, Mode=OneWay}" Foreground="{Binding WeeklyBrush}"/></StackPanel>
      <StackPanel Grid.Column="1" Margin="0,0,20,0"><TextBlock Text="{Binding ShortWindowLabel}" Style="{StaticResource Eyebrow}"/><StackPanel Orientation="Horizontal" Margin="0,5,0,8"><TextBlock Text="{Binding ShortWindowRemaining}" FontSize="20" FontWeight="SemiBold"/><TextBlock Text=" restant" Foreground="{StaticResource MutedBrush}" FontSize="10" VerticalAlignment="Bottom" Margin="0,0,0,3"/></StackPanel><ProgressBar Value="{Binding ShortWindowPercent, Mode=OneWay}" Foreground="#7C99DF"/></StackPanel>
      <StackPanel Grid.Column="2" Margin="0,0,15,0"><TextBlock Text="RESET HEBDOMADAIRE" Style="{StaticResource Eyebrow}"/><TextBlock Text="{Binding ResetExact}" FontSize="12" Margin="0,9,0,0"/><TextBlock Text="{Binding ResetCountdownWithZone}" Foreground="{StaticResource MutedBrush}" FontSize="10" Margin="0,5,0,0" TextTrimming="CharacterEllipsis" ToolTip="{Binding ResetZone}"/></StackPanel>
      <StackPanel Grid.Column="3"><TextBlock Text="RESETS EN RÉSERVE" Style="{StaticResource Eyebrow}"/><TextBlock Text="{Binding ReserveCount}" FontSize="20" FontWeight="SemiBold" Margin="0,5,0,0"/><TextBlock Text="{Binding CreditExpirySummary}" Foreground="{StaticResource MutedBrush}" FontSize="10" Margin="0,4,0,0" TextTrimming="CharacterEllipsis" ToolTip="{Binding CreditDetails}"/></StackPanel>
     </Grid>
     <Border Background="#3E2B26" CornerRadius="6" Padding="10,7" Margin="0,12,0,0" Visibility="{Binding HasError, Converter={StaticResource BoolVisibility}}"><TextBlock Text="{Binding Error}" TextWrapping="Wrap" Foreground="#EFB79D" FontSize="11"/></Border>
     <Grid Margin="0,14,0,0"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
      <WrapPanel><Button Content="{Binding SelectButtonText}" IsEnabled="{Binding CanSelect}" Tag="{Binding Id}" Click="Select_Click" Padding="10,6" FontSize="10" Margin="0,0,7,0"/><Button Content="Utiliser dans Codex ↗" IsEnabled="{Binding CanSwitch}" ToolTip="{Binding SwitchHint}" Tag="{Binding Id}" Click="Switch_Click" Padding="10,6" FontSize="10" Margin="0,0,7,0"/><Button Content="{Binding ConnectButtonText}" IsEnabled="{Binding CanConnect}" Tag="{Binding Id}" Click="Connect_Click" Padding="10,6" FontSize="10" Style="{StaticResource QuietButton}"/></WrapPanel>
      <StackPanel Grid.Column="1" Orientation="Horizontal"><Button Content="Détails" Tag="{Binding Id}" Click="Details_Click" Padding="9,6" FontSize="10" Style="{StaticResource QuietButton}"/><Button Content="···" ToolTip="Retirer ce compte" AutomationProperties.Name="Retirer ce compte du suivi" Tag="{Binding Id}" Click="Remove_Click" Padding="10,4" FontSize="14" Style="{StaticResource QuietButton}" IsEnabled="{Binding IsIdle}"/></StackPanel>
     </Grid>
    </StackPanel>
    </Border>
   </DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
   <TextBlock Text="Les pourcentages représentent le quota restant. Les données absentes restent indisponibles." Foreground="#637D99" FontSize="10" Margin="1,5,0,0" TextWrapping="Wrap"/>
  </StackPanel>
  </ScrollViewer>
  <Border Grid.Row="2" Background="#101925" BorderBrush="#243043" BorderThickness="0,1,0,0" Padding="22,0">
   <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><StackPanel Orientation="Horizontal" VerticalAlignment="Center"><Ellipse Width="6" Height="6" Fill="{Binding StatusBrush}" Margin="0,0,9,0"/><TextBlock Text="{Binding StatusText}" Foreground="#92A9BF" FontSize="10" TextTrimming="CharacterEllipsis" MaxWidth="700"/></StackPanel><TextBlock Grid.Column="1" Text="LOCAL · CHIFFRÉ · PRIVÉ" Foreground="#5F7A94" FontSize="9" VerticalAlignment="Center"/></Grid>
  </Border>
 </Grid>
 </Border>
</Window>
